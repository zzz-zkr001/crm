
syntax = "proto3";

package user_stats;
import "messages.proto";

service UserStatsService {
    rpc Query(QueryRequest) returns(stream QueryResponse) {
    }
    rpc RawQuery(RawQueryRequest) returns(stream RawQueryResponse) {
    }
}
